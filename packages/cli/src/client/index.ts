/**
 * CLI storage client module.
 *
 * Provides the bridge between CLI commands and storage implementations
 * using the callback-based IStorageClient interface.
 */

export { CLIStorageClient } from './CLIStorageClient';
export { StorageService } from './StorageService';
