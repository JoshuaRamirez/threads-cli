{
  "name": "threads-monorepo",
  "version": "0.0.0",
  "private": true,
  "description": "Threads platform monorepo - CLI, MCP server, and Firebase backend",
  "scripts": {
    "build": "turbo run build",
    "test": "turbo run test",
    "typecheck": "turbo run typecheck",
    "clean": "turbo run clean",
    "dev": "turbo run dev"
  },
  "devDependencies": {
    "turbo": "^2.7.5",
    "typescript": "^5.9.3"
  },
  "packageManager": "pnpm@9.14.4",
  "engines": {
    "node": ">=20.0.0"
  }
}
