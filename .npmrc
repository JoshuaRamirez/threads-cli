# npm registry configuration
registry=https://registry.npmjs.org/

# Enable provenance for npm publish (requires npm 9.5+)
provenance=true

# Access level for scoped packages
access=public

# Strict peer dependencies during install
strict-peer-dependencies=false

# Git checks for publishing
git-checks=true
